Read `docs/06_AGENT_HANDOFF.md`, `docs/05_DECISION_LOG.md`, and the Brielo Show-Up Trust project note before making changes.

Task:
Make the top notification bell button functional as a lightweight prototype notification drawer/sheet.

This is front-end prototype behavior only.

Do not add:

* backend
* database
* real auth
* WebSockets
* push notifications
* payments
* livestreaming
* AI matching
* major refactors

Do not rename folders or routes.

Problem:
The notification bell at the top has a red dot and looks tappable, but currently does nothing. That makes the prototype feel broken.

Expected behavior:
When the user taps the notification bell, open a simple bottom sheet or modal titled:

“Notifications”

Include a short list of mock notifications using Brielo language.

Example notifications:

* “Marcus L. joined Leg Day at XSport”
* “Priya S. sent a message in Sunday Coffee Run”
* “Your spot is saved for Grocery run”
* “Jordan K. would move again”
* “Move canceled. Chat closed.”

Use clean line icons only. No emoji icons.

Each notification should feel lightweight and social. Keep the UI consistent with Brielo’s dark card style, yellow accent, rounded cards, and mobile-first layout.

Add a simple empty state if needed:

“You’re all caught up.”

Do not create real push notifications.
Do not request notification permissions.
Do not add backend notification logic.
Do not add unread notification storage unless it is simple localStorage/mock state.

If the red dot is shown, tapping the notification drawer can clear the red dot for the current prototype session.

Quality check:
After changes, confirm:

* notification bell opens the sheet/modal
* sheet/modal can close
* app still runs
* Discover still loads
* joining a Move still works
* Leave Move still works
* Cancel Move still works
* Move Chat still works
* Profile Trust still works
* no old Rally/Brio/Plan/Crew language is reintroduced
* no emoji icons are added

Final goal:
The notification bell should feel clickable and complete enough for a prototype without adding real notification infrastructure.
