Create a landing page and waitlist flow for Brielo.

Branch:
landing-page-waitlist

Goal:
Make the root route `/` a public landing page for Brielo, while preserving access to the existing app/prototype.

Important:
Do not remove or break the existing prototype.
Do not change backend Phase 1 storage behavior.
Do not add real auth, database, API routes, payments, WebSockets, or external services.
This is a frontend/localStorage prototype task only.

Routing:
- `/` should show the new Brielo landing page.
- Existing app experience should remain available through `/discover`.
- If there is a current onboarding/root page, preserve it only if needed, but the public root should now be the landing page.

Landing page content:
Hero:
- Brielo
- LiveMore
- Less scrolling. More doing.
- Make real-life Moves with people who actually show up.

Primary CTA:
- Join the Waitlist

Secondary CTA:
- View Prototype

Sections:
1. What is Brielo?
Short explanation:
Brielo helps people turn free time into real life by making it easier to find casual activities, join Moves, and build a trusted Circle of people they would meet up with again.

2. How it works
- Make a Move
- Join a Move
- Build your Circle

3. Trust matters
Explain Show-Up Trust, host approval, request to join, and safety/reporting in simple language.

4. Waitlist form
Fields:
- Name
- Email
- City
- What would you use Brielo for? optional textarea

Behavior:
- Store waitlist entries in localStorage for now.
- Prevent empty email submission.
- Show success message after submission:
  “You’re on the Brielo waitlist.”
- Do not send emails.
- Do not connect external services yet.

Design:
- Use existing Brielo dark theme.
- Use black/dark background, yellow accent, white text.
- Include the glowing ring / LiveMore visual style if practical.
- Mobile-first.
- Clean, polished, simple.
- Do not overcomplicate animations.

Navigation:
- Add a top nav with Brielo and buttons/links:
  - View Prototype
  - Join Waitlist

Prototype access:
- “View Prototype” should route to `/discover`.

Validation:
After changes, run:
pnpm --filter @workspace/rally typecheck
PORT=5173 BASE_PATH=/ pnpm --filter @workspace/rally build

Then summarize:
- files changed
- routes changed
- how waitlist data is stored
- how to manually test