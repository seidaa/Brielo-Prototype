kind = "web"
previewPath = "/"
title = "Rally"
version = "1.0.0"
id = "artifacts/rally"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 8080

[services.development]
run = "PORT=5000 BASE_PATH=/ pnpm --filter @workspace/rally run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/rally", "run", "build" ]
publicDir = "artifacts/rally/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5000"
BASE_PATH = "/"
